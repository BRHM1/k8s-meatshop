apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app: frontend-deployment
spec:
  selector:
    matchLabels:
      tier: frontend
  template:
    metadata:
       labels:
         tier: frontend
    spec:
      containers:
        - name: frontend-container
          image: borhom11/frontend-meatshop:363d651820d818e954d5208a6b6118e1a1bfd06d
          volumeMounts:
          - name: config-volume
            mountPath: /usr/share/nginx/html/config.json
            subPath: config.json
      volumes:
        - name: config-volume
          configMap:
            name: frontend-config
            items:
              - key: config.json
                path: config.json
  replicas: 2
