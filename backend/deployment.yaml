apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: backend-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      tier: backend
  template:
    metadata:
      labels:
        tier: backend
    spec:
      initContainers:
        - name: run-migrations
          image: borhom11/meatshop-backend:9e37bc35a2f2966b78eb29d5c53ede487262f0da 
          command: ["sh", "-c"]
          args:
          - "python manage.py migrate --no-input && 
             python manage.py createcachetable" 
          envFrom:
          - secretRef:
              name: backend-secret 
      containers:
        - name: backend
          image: borhom11/meatshop-backend:9e37bc35a2f2966b78eb29d5c53ede487262f0da
          envFrom:
            - secretRef:
                name: backend-secret
