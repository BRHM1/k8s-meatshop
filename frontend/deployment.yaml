apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app: frontend-deployment
spec:
  selector:
    matchLabels:
      tier: frontend
  template:
    metadata:
       labels:
         tier: frontend
    spec:
      containers:
        - name: frontend-container
          image: borhom11/frontend-meatshop:7ccd7ed8c0abe335d91563c7893f77823e8942f5
          volumeMounts:
          - name: config-volume
            mountPath: /usr/share/nginx/html/config.json
            subPath: config.json
      volumes:
        - name: config-volume
          configMap:
            name: frontend-config
            items:
              - key: config.json
                path: config.json
  replicas: 2
